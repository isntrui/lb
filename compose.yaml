services:
  postgres:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=${DBUSER}
      - POSTGRES_PASSWORD=${DBPASS}
      - POSTGRES_DB=lbdb
    user: postgres
  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - postgres
    environment:
      - SPRING_DATASOURCE_URL=${DBNAME}
      - SPRING_DATASOURCE_USERNAME=${DBUSER}
      - SPRING_DATASOURCE_PASSWORD=${DBPASS}
    ports:
      - '80:80'

